# Evaluation Results

This directory contains pre-computed evaluation results.

The files follow the format of `<TARGET>.json` where `<TARGET>` is the target end date used for the evaluation.

Evaluation is done using the arguments specified in the [Trading Guide](../TRADING.md).
